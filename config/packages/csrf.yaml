# Enable stateless CSRF protection for forms and logins/logouts
framework:
    form:
        csrf_protection:
            token_id: submit

    csrf_protection:
        stateless_token_ids:
            - submit
            - authenticate
            # logout intentionnellement absent : le token doit être lié à la session
            # pour éviter que la déconnexion se déclenche sans session valide
