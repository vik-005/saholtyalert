{# Alias de rétrocompatibilité pointant vers _stat_card.html.twig #}
{% include 'components/_stat_card.html.twig' with {
    title: title|default(''),
    value: value|default(0),
    icon: icon|default('bi-clipboard-data'),
    color: color|default('teal'),
    trend_val: trend_val|default(subtitle|default('')),
    trend_label: trend_label|default(''),
    trend_dir: trend_dir|default('up')
} %}
