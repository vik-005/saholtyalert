<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>Registre Consolidé GEI</title>
    <style>
        @page {
            margin: 0;
            @bottom-center {
                content: "{{ platform_name }} — {{ platform_full }} | Document non contrôlé — {{ date_impression|date('d/m/Y H:i') }} | Page " counter(page) " / " counter(pages);
                font-family: Helvetica, Arial, sans-serif;
                font-size: 7px;
                color: #64748b;
                padding-bottom: 8mm;
            }
        }

        * { box-sizing: border-box; }

        body {
            font-family: Helvetica, Arial, sans-serif;
            font-size: 8px;
            color: #1e293b;
            margin: 0;
            padding: 0;
        }

        /* ═══════════════════════════════════════════════
           HEADER OFFICIEL — Bandeau vert institutionnel GEI
           ═══════════════════════════════════════════════ */
        .doc-header {
            background: linear-gradient(135deg, #0d5f4a 0%, #16856b 60%, #1ca882 100%);
            color: #ffffff;
            padding: 0;
        }
        .header-inner {
            display: flex;
            align-items: center;
            padding: 12px 16px;
            gap: 14px;
        }
        .header-logo {
            width: 44px;
            height: 44px;
            background: rgba(255,255,255,0.15);
            border: 2px solid rgba(255,255,255,0.3);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 18px;
            font-weight: 900;
            color: #ffffff;
            flex-shrink: 0;
            letter-spacing: -1px;
        }
        .header-text {
            flex: 1;
        }
        .header-platform {
            font-size: 7px;
            text-transform: uppercase;
            letter-spacing: 2.5px;
            color: rgba(255,255,255,0.75);
            margin-bottom: 2px;
            font-weight: 600;
        }
        .header-platform-name {
            font-size: 11px;
            font-weight: 800;
            color: #ffffff;
            letter-spacing: 0.3px;
            margin-bottom: 1px;
        }
        .header-title {
            font-size: 14px;
            font-weight: 900;
            margin: 0;
            color: #ffffff;
            letter-spacing: 0.5px;
        }
        .header-meta {
            text-align: right;
            flex-shrink: 0;
        }
        .header-count {
            font-size: 20px;
            font-weight: 900;
            color: #c9f0e3;
            line-height: 1;
        }
        .header-count-label {
            font-size: 8px;
            color: rgba(255,255,255,0.65);
            margin-top: 2px;
        }
        .header-date-meta {
            font-size: 7.5px;
            color: rgba(255,255,255,0.6);
            margin-top: 4px;
        }
        /* Barre accent décorative */
        .header-accent-bar {
            height: 3px;
            background: linear-gradient(90deg, #c9f0e3 0%, #45e3b8 50%, rgba(255,255,255,0.15) 100%);
        }

        /* ═══════════════════════════════════════════════
           META BAR
           ═══════════════════════════════════════════════ */
        .meta-bar {
            background: #f0faf5;
            border-bottom: 2px solid #c9e8de;
            padding: 5px 16px;
            font-size: 7.5px;
            color: #0d5f4a;
            display: flex;
            gap: 20px;
        }
        .meta-label {
            font-weight: 800;
            color: #0d5f4a;
            margin-right: 4px;
        }

        /* ═══════════════════════════════════════════════
           TABLEAU REGISTRE
           ═══════════════════════════════════════════════ */
        .doc-body {
            padding: 10px 14px;
        }
        table.registre {
            width: 100%;
            border-collapse: collapse;
            font-size: 8px;
        }
        table.registre thead th {
            background: #16856b;
            color: #ffffff;
            padding: 7px 8px;
            text-align: left;
            font-size: 7.5px;
            text-transform: uppercase;
            letter-spacing: 0.8px;
            font-weight: 800;
            border-bottom: 2px solid #0d5f4a;
            white-space: nowrap;
        }
        table.registre tbody td {
            padding: 5px 8px;
            border-bottom: 1px solid #e2f0ec;
            vertical-align: top;
            color: #1e293b;
        }
        table.registre tbody tr:nth-child(even) {
            background: #f5faf8;
        }
        .row-critique { background: #fef2f2 !important; }
        .row-eleve { background: #fff7ed !important; }
        .row-modere { background: #fffbeb !important; }
        .row-faible { background: #f0fdf4 !important; }

        .badge {
            display: inline-block;
            padding: 2px 5px;
            border-radius: 2px;
            font-weight: bold;
            font-size: 7px;
            text-transform: uppercase;
            letter-spacing: 0.4px;
        }
        .badge-critique { background: #fee2e2; color: #991b1b; }
        .badge-eleve { background: #ffedd5; color: #9a3412; }
        .badge-modere { background: #fef3c7; color: #92400e; }
        .badge-faible { background: #d1fae5; color: #065f46; }

        .score-val {
            font-weight: bold;
            font-size: 9px;
        }
        .score-critique { color: #dc2626; }
        .score-eleve { color: #ea580c; }
        .score-modere { color: #d97706; }
        .score-faible { color: #059669; }

        .code-gei {
            font-family: monospace;
            font-weight: bold;
            color: #0f172a;
            font-size: 9px;
        }

        .empty-state {
            text-align: center;
            padding: 20px;
            color: #94a3b8;
            font-style: italic;
        }
    </style>
</head>
<body>

    <!-- ═══════════════════════════════════════════════
         HEADER OFFICIEL
         ═══════════════════════════════════════════════ -->
    <div class="doc-header">
        <div class="header-inner">
            <div class="header-logo">GEI</div>
            <div class="header-text">
                <div class="header-platform">Groupe d'Échange d'Informations — Renseignement Opérationnel</div>
                <div class="header-platform-name">Plateforme GEI</div>
                <div class="header-title">REGISTRE CONSOLIDÉ D'ALERTES</div>
            </div>
            <div class="header-meta">
                <div class="header-count">{{ alerts|length }}</div>
                <div class="header-count-label">alerte{{ alerts|length > 1 ? 's' : '' }}</div>
                <div class="header-date-meta">{{ date_impression|date('d/m/Y H:i') }}</div>
            </div>
        </div>
        <div class="header-accent-bar"></div>
    </div>

    <!-- ═══════════════════════════════════════════════
         META BAR
         ═══════════════════════════════════════════════ -->
    <div class="meta-bar">
        <span><span class="meta-label">Document :</span>Registre consolidé</span>
        <span><span class="meta-label">Imprimé :</span>{{ date_impression|date('d/m/Y H:i') }}</span>
        <span><span class="meta-label">Classification :</span>Confidentiel</span>
        <span><span class="meta-label">Alertes :</span>{{ alerts|length }}</span>
    </div>

    <!-- ═══════════════════════════════════════════════
         TABLEAU
         ═══════════════════════════════════════════════ -->
    <div class="doc-body">
        <table class="registre">
            <thead>
                <tr>
                    <th style="width: 8%;">Code GEI</th>
                    <th style="width: 7%;">Date</th>
                    <th style="width: 10%;">Zone</th>
                    <th style="width: 12%;">Corridor</th>
                    <th style="width: 13%;">Catégorie</th>
                    <th style="width: 22%;">Résumé Exécutif</th>
                    <th style="width: 5%; text-align: center;">Score</th>
                    <th style="width: 7%;">Priorité</th>
                    <th style="width: 12%;">Statut</th>
                </tr>
            </thead>
            <tbody>
                {% for alert in alerts %}
                    {% set p = alert.effectiveNiveauPriorite %}
                    {% set scoreClass = '' %}
                    {% if p %}
                        {% set scoreClass = 'row-' ~ p.value %}
                    {% endif %}
                    <tr class="{{ scoreClass }}">
                        <td><span class="code-gei">{{ alert.codeGei ?? 'BROUILLON' }}</span></td>
                        <td>{{ alert.dateCreation|date('d/m/Y') }}</td>
                        <td>{{ alert.market.nom }}</td>
                        <td>{{ alert.portCorridor ?: '-' }}</td>
                        <td>{{ alert.categorie ?: '-' }}</td>
                        <td style="max-width: 280px;">{{ alert.resumeExecutif|slice(0, 110) }}{% if alert.resumeExecutif|length > 110 %}…{% endif %}</td>
                        <td style="text-align: center;">
                            <span class="score-val
                                {% if alert.effectiveScore >= 18 %}score-critique
                                {% elseif alert.effectiveScore >= 14 %}score-eleve
                                {% elseif alert.effectiveScore >= 10 %}score-modere
                                {% else %}score-faible{% endif %}">
                                {{ alert.effectiveScore ?? '-' }}
                            </span>
                        </td>
                        <td>
                            {% if p %}
                                <span class="badge badge-{{ p.value }}">{{ p.label() }}</span>
                            {% else %}-{% endif %}
                        </td>
                        <td>{{ alert.statut ? alert.statut.label() : '-' }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="9" class="empty-state">Aucune alerte enregistrée.</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

</body>
</html>
